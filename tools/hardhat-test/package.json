{
  "name": "etherlang-hardhat-test",
  "version": "0.2.3",
  "description": "Hardhat tests that exercise the etherlang node's EVM via eth_call state overrides",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^4.0.0",
    "@types/node": "^22.0.0",
    "ethers": "^6.13.0",
    "hardhat": "^3.17.0",
    "typescript": "^5.6.0"
  },
  "type": "module"
}
